1) what is database and relational database?
A database is a structured collection of data that is stored and managed electronically. It allows for
storage,retrival,updating,deleting.
“A database is a system that stores and manages data. A relational database is a type of database that stores data in tables with rows and columns and uses keys to relate information between tables. 
It ensures data is structured and consistent. Common relational databases include MySQL and PostgreSQL.”

2)